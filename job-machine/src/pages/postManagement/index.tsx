import { TreeTable } from '../../components/table/treeTable';
import { Card } from 'antd';

const Post = () => {
  return (
    <Card id='tree-table'>
      <TreeTable />
    </Card>
  );
};

export default Post;
